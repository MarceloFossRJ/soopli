<%= form_for(rag_status) do |f| %>
  <% if rag_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rag_status.errors.count, "error") %> prohibited this rag_status from being saved:</h2>

      <ul>
      <% rag_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
